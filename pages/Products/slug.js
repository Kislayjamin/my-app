import { useRouter} from "next/router";

const post = () => {
   // const router = useRouter()
   // const { slug } = router.query

    // return <a>Post: {pid}</a>
    return (
        <div>
            Slug
        </div>
    )
}

export default Post